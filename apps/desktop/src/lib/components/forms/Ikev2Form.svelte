<script lang="ts">import type { InstanceForm } from "../../instance-form"; export let form: InstanceForm;</script>
<div class="form-grid equal"><label>Server certificate identity<input bind:value={form.ikev2_server_identity} required placeholder={form.endpoint_host || "vpn.example.com"} /></label><label>Client certificate lifetime (days)<input type="number" bind:value={form.ikev2_certificate_lifetime_days} min="1" max="825" required /></label></div><p class="help">IKEv2 always reserves UDP 500 and 4500. Client exports are password-protected PKCS#12 credentials.</p>
